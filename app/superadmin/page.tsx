import { requireUser } from "@/lib/auth/session";
import { prisma } from "@/lib/db";
import { Building2, CalendarDays, UsersRound, CircleDollarSign } from "lucide-react";

export default async function SuperAdmin(){await requireUser(["SUPERADMIN"]);const [tenants,tenantCount,appointmentCount,userCount]=await Promise.all([prisma.tenant.findMany({orderBy:{createdAt:"desc"},include:{_count:{select:{appointments:true,users:true,customers:true}}}}),prisma.tenant.count(),prisma.appointment.count(),prisma.user.count()]);return <div className="app-shell" style={{gridTemplateColumns:"1fr"}}><main className="content" style={{maxWidth:1280,width:"100%",margin:"auto"}}><div className="page-title"><div><div className="brand"><span className="brand-mark">L+</span><span>SuperAdmin</span></div><h1 style={{marginTop:18}}>Platforma pod kontrolom.</h1></div><a className="btn" href="/dashboard">Natrag na dashboard</a></div><section className="stats"><Stat icon={<Building2/>} label="Aktivne firme" value={String(tenantCount)}/><Stat icon={<CalendarDays/>} label="Ukupno termina" value={String(appointmentCount)}/><Stat icon={<UsersRound/>} label="Korisnički računi" value={String(userCount)}/><Stat icon={<CircleDollarSign/>} label="MRR" value="—"/></section><div className="table-card scroll-x"><div className="card-head"><strong>Firme / tenant-i</strong><button className="btn btn-brand">+ Dodaj firmu</button></div><table className="super-table"><thead><tr><th>Firma</th><th>Status</th><th>Korisnici</th><th>Klijenti</th><th>Termini</th><th>Booking URL</th></tr></thead><tbody>{tenants.map(t=><tr key={t.id}><td><strong>{t.name}</strong><div className="muted" style={{fontSize:12}}>{t.email}</div></td><td><span className={`pill ${t.status==="TRIAL"?"pending":""}`}>{t.status}</span></td><td>{t._count.users}</td><td>{t._count.customers}</td><td>{t._count.appointments}</td><td><a href={`/book/${t.slug}`} style={{color:"#5147d4",fontWeight:700}}>/book/{t.slug}</a></td></tr>)}</tbody></table></div></main></div>}
function Stat({icon,label,value}:{icon:React.ReactNode;label:string;value:string}){return <div className="stat"><div className="feature-icon" style={{margin:0,width:36,height:36}}>{icon}</div><div className="stat-label" style={{marginTop:12}}>{label}</div><div className="stat-value">{value}</div></div>}
